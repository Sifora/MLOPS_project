name: CI/CD for Flask ML App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run model test
      run: python model.py

    - name: Build Docker image
      run: docker build -t my-flask-ml-app .

    # Optional: push image to Docker Hub
    # - name: Push to Docker Hub
    #   env:
    #     DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
    #     DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    #   run: |
    #     echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
    #     docker tag my-flask-ml-app $DOCKER_USERNAME/my-flask-ml-app:latest
    #     docker push $DOCKER_USERNAME/my-flask-ml-app:latest
